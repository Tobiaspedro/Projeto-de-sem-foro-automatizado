# Projeto-de-semáforo digital
//este progeto é uma lógica que utiliza como fonte de programação o  microcontrolador Arduino UNO, essa lógica tem como obetivo representar 
de forma detalhada e expressiva a composição lógica de um semáforo real.
//Materiasis utilizados:
// uma protoboard;
//3 led's ( um vermelho, um amarelo e um verde);
//Qquatro fios jumper;
//3 resistores de 100 ohms;
//Um cabo USB;
// Um microcontrolador  ARDUINO;
//É necessário que a  IDE do arduino esteja em blink ;
 
  Instalação do Projeto:
 
 // Void setup, assim que o arduino é ligado ele é execultado, apenas uma vez.
 void setup() {
 pinMode(8, OUTPUT);     define o pino 8 como saída
 pinMode(9, OUTPUT);     define o pino 9 como saída
 pinMode(10,OUTOUT);     define o pino 10 como saída
 }
 //void loop. é execultado enquanto o arduino estiver ligado.
 void loop() {
 //controle do led verde
 digitalWrite(8,HIGH);   //acende o led
 delay(5000);            //espera 5 segundos
 digitalWrite(8,LOW);    //apaga o led
 
//controle do led amarelo
digitalWrite(9,HIGH);    //acende o led
delay(2000);             //espera 2 segundos
digitalWrite(9,LOW);     //apaga o led

//controle do led vermelho
digitalWrite(10,HIGH);   //acende o led
delay(5000);             //espera 5 segundos
digitalWrite(10,LOW);    //apaga o led
}

//Essa lógica tem aplicação na maontagem do semáforo real e na automação do processo


 
